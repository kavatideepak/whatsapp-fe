/**
 * useTheme Hook
 * 
 * Re-export from ThemeContext for backward compatibility
 * All theme logic is now in ThemeContext for centralized management
 */

export { useTheme, THEME_COLORS } from '@/context/ThemeContext';

